#!/usr/bin/env bash
# git-wt-bootstrap: `git-wt bootstrap` under the name git aliases, the agent sidebar and
# Claude Code settings already use. See git-wt.
exec "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/git-wt" bootstrap "$@"

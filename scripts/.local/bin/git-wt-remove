#!/usr/bin/env bash
# git-wt-remove: `git-wt remove` under the name git aliases, the agent sidebar and
# Claude Code settings already use. See git-wt.
exec "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/git-wt" remove "$@"
